import { Typography } from '@mui/material'
import { loading } from './styles'

const Loading = () => {
	return (
		<Typography variant='h3' sx={loading}>
			Loading
		</Typography>
	)
}

export default Loading
